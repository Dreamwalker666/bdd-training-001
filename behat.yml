default:
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
            browser_name: chrome
            base_url:  'http://localhost:8000'
            sessions:
                default:
                    symfony2: ~
                symfony:
                    symfony2:
                webdriver:
                    selenium2: ~


    suites:
        default:
            contexts:
              - Direct\FeatureContext:
        script:
            contexts:
              - Script\FeatureContext:
        web:
            contexts:
              - Web\FeatureContext:
